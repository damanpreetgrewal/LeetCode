/**
 * @param {number} columnNumber
 * @return {string}
 */
var convertToTitle = function (columnNumber) {
  let output = 0;
  let length = columnNumber.length;
  console.log(length);

  console.log(columnNumber[length - 1].charCodeAt());
};

console.log(convertToTitle('AA'));
